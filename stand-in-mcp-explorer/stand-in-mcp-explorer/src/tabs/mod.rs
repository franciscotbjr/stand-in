//! Content tabs region — Tools, Resources, Prompts, Notifications, History.
//!
//! - `work_area` (M8): work area shell — split/single scaffolds + the §5b scroll chain.
//! - `tools` (M9): Tools tab — live search + selection + params from schema (compose).
//!   Preenchidas no M11 (Resources), M12 (Prompts),
//!   M13 (Notifications), M14 (History).

pub mod history;
pub mod notifications;
pub mod prompts;
pub mod resources;
pub mod tools;
pub mod work_area;
