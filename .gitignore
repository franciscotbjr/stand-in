/target
/stand-in-mcp-explorer/target
/sync-crates/target

Cargo.lock
.openclaude-profile.json
impl.md
prompt.md
.claude/settings.local.json
.claude/settings.local.example.json

# Transient multi-agent-flow poll signal files (poll-flow.ps1)
.stateful-spec/poll-result*.txt
.claude/scheduled_tasks.lock
.stateful-spec/spawn-result.txt
# Transient design-gate verdict file (architect writes it; PM reads + executes the verb; 031 protocol)
.stateful-spec/arch-verdict.md

# Stray nested .stateful-spec/ created when the engineer runs from the wrong cwd
# (the excluded explorer sub-workspace) — never commit the orphan copy (F4/M13).
# The fix is to always run flow/the gate from the repo root via --manifest-path.
stand-in-mcp-explorer/.stateful-spec/

# Windows reserved-name artifact created by stray `> nul` redirects
nul
.claude/settings.json

# Perf measurement logs (037 / O-024) — JSONL emitted by `--capture <region> perf`
mcpx-perf-*.jsonl
